// SQLite datasource 头
// 由 prisma/scripts/build-schema.ts 在 DATABASE_PROVIDER=sqlite 时拼接使用

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
