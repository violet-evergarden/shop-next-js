// MySQL datasource 头
// 由 prisma/scripts/build-schema.ts 在 DATABASE_PROVIDER=mysql 时拼接使用

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
